# 开发日志 — 2026-06-10

## 今日完成
- [x] 修复置顶记录在渲染层被"今天/更早"时间分组压到下方的问题
- [x] 新增顶部"置顶记录"分组，置顶卡片始终优先显示且不进入"更早的记录"分组
- [x] 新增 `src/renderer/list-groups.js`，将列表分组规则抽成可测试的纯函数
- [x] 修复 `cleanExpired()` 删除数量在保存数据库后变为 0 的问题
- [x] 补充置顶分组和过期清理回归测试
- [x] 更新 `docs/设计规范.md` 中的置顶分组说明
- [x] 为"今天"分组补充分组标签，避免置顶记录和当天普通记录视觉混淆
- [x] 按用户要求刷新打包产物：安装包、blockmap 和 `dist/win-unpacked`

## 遇到的问题
- 置顶数据查询本身已按 `pinned DESC, created_at DESC` 排序，但渲染层重新按时间切分列表，导致旧置顶记录仍被放进"更早的记录"。
- `sql.js` 写盘导出后会影响 `SELECT changes()` 结果，删除数量需要在 `save()` 前读取。
- "今天"分组原本存在，但渲染层故意跳过了它的标签；置顶分组出现后，这会让两组内容紧挨在一起。

## 待办事项
- [ ] 用户在实际面板中确认置顶分组视觉和滚动位置符合预期

## 明日计划
- 根据用户反馈继续修正面板列表体验

## 验证
- [x] `node --test test\renderer-groups.test.js`
- [x] `node --test test\store-retention.test.js`
- [x] `node --check src\renderer\list-groups.js`
- [x] `node --check src\renderer\app.js`
- [x] `node --check src\main\store.js`
- [x] `npm test` — 16/16 通过
- [x] 追加验证：`npm test` — 17/17 通过
- [x] 打包前验证：`npm test` — 17/17 通过
- [x] `npm run build` — 成功生成 `dist\历史粘贴板 Setup 1.0.0.exe`
- [x] `dist\win-unpacked\历史粘贴板.exe --autostart` 冒烟验证：启动后持续运行 5 秒，随后停止测试进程
